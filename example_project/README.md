# Example Project Workspace

This is a simplified, minimal codebase containing sample PHP controllers and Python scripts to demonstrate local vector indexing and semantic search capabilities.

## Structure
- `app/Http/Controllers/UserController.php`: Standard PHP controller registering users and hashing credentials.
- `scripts/data_processor.py`: Python class parsing metrics and calculating averages.
